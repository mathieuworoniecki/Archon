# Archon — Digital Investigation Platform
# Copy this file to .env and fill in your values
#
# Generate a secure JWT secret:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"

# ============================================
# REQUIRED: API Keys & Secrets
# ============================================
# Get your Gemini key at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# JWT secret for authentication (REQUIRED when auth is enabled)
JWT_SECRET_KEY=

# ============================================
# OPTIONAL: Paths & Configuration
# ============================================
DOCUMENTS_PATH=./documents

# Set to true ONLY for local development (bypasses all JWT checks)
DISABLE_AUTH=false

# Comma-separated list of allowed CORS origins
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:3100

# ============================================
# PRODUCTION ONLY: Database & Service Secrets
# ============================================
# These are used by docker-compose.prod.yaml
# POSTGRES_USER=archon
# POSTGRES_PASSWORD=<strong-random-password>
# POSTGRES_DB=archon
# MEILISEARCH_API_KEY=<strong-random-key>
# FLOWER_USER=admin
# FLOWER_PASSWORD=<strong-random-password>
